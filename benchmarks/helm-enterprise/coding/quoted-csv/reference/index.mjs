export function solve(text){if(!text)return [];const rows=[];let row=[],cell='',quoted=false,closed=false;for(let i=0;i<text.length;i++){const c=text[i];if(quoted){if(c==='"'){if(text[i+1]==='"'){cell+='"';i++}else{quoted=false;closed=true}}else cell+=c;continue}if(c===','||c==='\n'||c==='\r'){row.push(cell);cell='';closed=false;if(c!==','){rows.push(row);row=[];if(c==='\r'&&text[i+1]==='\n')i++}continue}if(closed)throw Error('junk');if(c==='"'){if(cell)throw Error('quote');quoted=true}else cell+=c}if(quoted)throw Error('unclosed');if(cell||closed||row.length||text.endsWith(',')){row.push(cell);rows.push(row)}return rows}
