# quoted-csv

Input CSV string. Return rows of fields. Quoted fields support embedded commas/newlines and doubled quotes; CRLF/LF accepted; preserve empty cells; trailing newline adds no row; empty input gives []. Reject unclosed quotes or junk after closing quotes.

Export solve from index.mjs. Use Node built-ins only; preserve inputs. Change only the provided code files.
