export function solve({total,weights}){if(!Number.isSafeInteger(total)||total<0||!weights.length||weights.some(w=>!Number.isFinite(w)||w<0))throw Error('invalid');const sum=weights.reduce((a,b)=>a+b,0);if(!sum)throw Error('invalid');const raw=weights.map(w=>total*w/sum),out=raw.map(Math.floor),order=raw.map((v,i)=>({i,r:v-out[i]})).sort((a,b)=>b.r-a.r||a.i-b.i);for(let n=total-out.reduce((a,b)=>a+b,0),i=0;i<n;i++)out[order[i].i]++;return out}
