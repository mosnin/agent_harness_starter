export async function solve(jobs,signal){if(jobs.some(j=>!Number.isFinite(j.delayMs)||j.delayMs<0))throw Error('delay');const completed=[];for(const j of jobs){if(signal.aborted)return {status:'cancelled',completed};await new Promise(resolve=>{let timer;const finish=()=>{clearTimeout(timer);signal.removeEventListener('abort',finish);resolve()};timer=setTimeout(finish,j.delayMs);signal.addEventListener('abort',finish,{once:true})});if(signal.aborted)return {status:'cancelled',completed};completed.push(j.id)}return {status:'completed',completed}}
