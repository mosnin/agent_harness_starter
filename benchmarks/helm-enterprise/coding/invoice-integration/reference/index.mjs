import {price} from './pricing.mjs';export function solve({lines,discountBps,taxBps}){if([discountBps,taxBps].some(n=>!Number.isInteger(n)||n<0||n>10000))throw Error('rate');const items=price(lines),subtotal=items.reduce((n,l)=>n+l.cents,0);if(!Number.isSafeInteger(subtotal))throw Error('total');const discount=Math.floor(subtotal*discountBps/10000+0.5),tax=Math.floor((subtotal-discount)*taxBps/10000+0.5);return {subtotal,discount,tax,total:subtotal-discount+tax,items}}
