export function solve(events){let s={status:'queued',id:null,result:null};for(const e of events){if(e.type==='start'&&!['running','unknown'].includes(s.status))s={status:'running',id:e.id,result:null};else if(e.id===s.id){if(e.type==='interrupt'&&s.status==='running')s={...s,status:'unknown'};else if((e.type==='finish'&&s.status==='running')||(e.type==='reconcile'&&s.status==='unknown'))s={...s,status:'completed',result:e.result}}}return s}
