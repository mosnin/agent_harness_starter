export function solve({tasks,limit}){if(!Number.isInteger(limit)||limit<1||limit>8)throw Error('limit');const ids=new Set(tasks.map(t=>t.id));if(ids.size!==tasks.length||tasks.some(t=>t.dependsOn.some(id=>!ids.has(id))))throw Error('ids');const done=new Set(),out=[];while(done.size<tasks.length){const ready=tasks.filter(t=>!done.has(t.id)&&t.dependsOn.every(id=>done.has(id))).slice(0,limit);if(!ready.length)throw Error('cycle');out.push(ready.map(t=>t.id));ready.forEach(t=>done.add(t.id))}return out}
