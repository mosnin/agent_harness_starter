# Stored and Scalar OAuth account data

> Current addendum: Stored now has reviewed operator-only provisioning source for sto_client_hades_desktop_v1, with 21 isolated tests. No provisioning was executed. The exact patch could not be placed on a remote branch because the GitHub connector required unavailable approval; see stored-remote-delivery-receipt.json. Original inventory below is historical; ACCEPTANCE_V2.md records the current gates.

Source candidates only. Neither provider was deployed, migrated or connected to a real account. No listener, live provider, database service or native application was launched. The OAuth issuers already existed; this work connects their grants to narrow data projections.

## Scalar

Actual local checkout `/Users/preston/Documents/Codex/2026-09-06/sicarii-ui-reference`, clean starting revision `7922be8b22e6f7cd3e6041ddecfeceac8f0a4414`, origin mosnin/Sicarii. Changes remain uncommitted. Read AGENTS.md; no UI changed.

The user-specified origin is **https://tryscalar.xyz**. An earlier scalar.sh suggestion was unverified and must not be used to authorize OAuth. Source metadata derives issuer from getPublicOrigin(request); this does not verify a live custom-domain deployment. The blocked tryscalar.xyz lookup was not retried through another path.

Existing machine routes: /oauth/register, /oauth/token, /oauth/revoke, /oauth/userinfo; human route /oauth/authorize. DCR now explicitly permits only the additional private URI `ai.hades.desktop:/oauth/scalar`. Scopes: openid profile crm:read crm:write. Userinfo account normalization: id=sub, tenantId=workspace.id, name=workspace.name or name. Removed workspace membership now refuses token authentication and userinfo instead of inventing a member role.

New GET `/api/oauth/contacts?page=<last UUID>` returns `{records:[{id,collection:"contacts",title,revision,updatedAt,data:{name,notes}}],nextPage?}`. Read scope crm:read; queries use the OAuth accountId, not the human subject ID. Pagination is keyset ID order,100 records per page, not a frozen snapshot or durable change feed. Full snapshot polling remains the honest client mode.

New POST to that endpoint accepts `{key:UUID,id:UUID,expectedRevision:64hex,operation:"update",data:{name?,notes?}}`. Omit generic client collection from the body. Revision is SHA256 over id, timestamp, name and notes, avoiding same-millisecond stale edits. Strict input rejects authority fields. Serializable transaction checks account+revision, conditionally updates matching projected fields and timestamp, and stores exact response in OauthDataWrite. Returns `{status:"applied",record}`. Changed key input409, stale revision412, absent/foreign404, missing scope403. Duplicate retained request returns its prior result without reapplying. No create/delete/enrichment capability is added.

Changed files: src/lib/oauth-contact-data.ts; src/app/api/oauth/contacts/route.ts; src/lib/oauth-server.ts; src/app/oauth/register/route.ts; src/app/oauth/userinfo/route.ts; prisma/schema.prisma; tests/oauth-contact-data.test.ts; tests/oauth-native-client.test.ts. Additive OauthDataWrite Prisma model requires schema deployment and regenerated Prisma client before enabling writes. No production migration was run.

Evidence: 30/30 tests across oauth-server, oauth-contact-data, oauth-native-client passed with in-process Prisma fixtures. Command: `vitest run tests/oauth-contact-data.test.ts tests/oauth-server.test.ts tests/oauth-native-client.test.ts --minWorkers=1 --maxWorkers=1`; log /tmp/scalar-oauth-tests.log. No real transaction/deployment acceptance. ESLint could not run: installed8.57.1 lacks exported eslint/config required by repo config. No install or broad build attempted. Future gate: actual PostgreSQL serializable concurrent-write/idempotency race, generated delegate, membership removal, refreshed-scope refusal and authenticated native round-trip.

## Stored

No complete local checkout was found. Approved read-only GitHub connector captured a bounded source slice at `mosnin/agentwiki@37ef3dfb155e7169b866fced81675dc886676b24`. Review artifact directory: `/Users/preston/Documents/Codex/2026-09-10/stored-oauth-source-patch` contains original/, source/, stored-oauth.patch and MANIFEST.json. **This is not a full checkout, applied remote change or deployment.** All20 captured original files match their exact Git blob SHA; no runtime assets/models downloaded. Root and web AGENTS.md absent at the pin; canonical CLAUDE.md read.

Existing Stored issuer uses configured application URL. Existing /oauth/authorize, /oauth/token, /oauth/revoke, /oauth/userinfo; static client registration only. Operator must run the existing registration script with exact `ai.hades.desktop:/oauth/stored`, scopes `openid profile org:read memory:read memory:write`, then supply returned public client ID. No client ID was fabricated or registration performed. Stored is the user-specified stored.to service; live issuer identity remains unverified.

New source-patch route GET `/api/v1/oauth/memories?page=<integer>` uses OAuth memory:read, current active membership/role, account call quota/rate-limit and organization-specific engine placement. It refuses mock or unreachable engine data. Returns the same normalized record envelope with collection memories, data `{content,summary,source}`, opaque64hex revision and nextPage. POST same route accepts `{key:string,id:string,expectedRevision:64hex,operation:"update",data:{content:string}}`; key1-100 alphanumeric/underscore/hyphen, content<=64KB. Scope memory:write and current membership required. Project and actor are host-derived from the grant, never user-selected.

Atomic engine support is a new POST `/api/memories/:id/conditional` behind existing service-token and tenant middleware. SQLite BEGIN IMMEDIATE reads current content+timestamp revision and retained actor/key receipt, calls actual updateMemoryFromVault, and commits row/backlinks plus exact mutation receipt together. Embedding/event callbacks are deferred until commit. Lost acknowledgement repeats return the retained result; changed input refuses409; current mismatch412. PostgreSQL rejects501 before database access; mock/other adapters without optional conditionalUpdateMemory reject501. No unsafe read-then-write emulation. Existing unconditional APIs retain their original behavior.

Six deterministic tests execute the new helper using actual Node SQLite (Bun-compatible adapter surface): CAS/receipt once, same-second drift, insertion-failure rollback with no effects, unsupported backend refusal, tenant isolation, malformed/missing inputs. Log /tmp/stored-atomic-tests.log. Changed TS files parse via esbuild. Full Bun/Hono engine, web build, live PostgreSQL and end-to-end endpoint tests remain unverified. The patch intentionally does not claim a durable change feed; full snapshots poll. Follow-up gates: apply patch to full pinned checkout, run native Bun SQLite/backlinks/embedding cancellation tests, review quota/error mapping, static native client registration, deploy web+engine together and execute authenticated read/write/revoke tests.
