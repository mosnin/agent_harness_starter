# Corrected reference dossier — governed v1

Both verified-launcher operations succeeded: `build_governed_packet` and `prepare_semantic_review`. Kernel result: **needs_evidence**, semantic review pending, original objective not accepted. All 27 input claims are included. No fetched source was executed and no new acquisition occurred.

Review **[semantic-review-dossier.json](semantic-review-dossier.json)**, which is the complete actual `prepare-response.json.result`, not an abbreviated packet. Its exact digest is `c7b24f6f277716c0ff01ae1718bbe91315c01334db154a451eda3701a0c06145`. The completed final archive is [archive-final](archive-final/receipt.json). The earlier `archive` and pre-lineage-correction responses are superseded but preserved; do not bind a review to them.

The original reviewed report remains unchanged at `../reference-harnesses.md`, SHA256 `1c5b1a6872a8b56277375c762b379a079e7c1ba51e50a0b0020eefd80066cd4e`, with an exact copy here. The [corrected report](reference-harnesses.md) narrows C03 to owner-provided reload, C04 to configured residency with unbounded fallback, C08 to a prompt threshold rather than a proved stop, preserves Cowork local/cloud contradictions, and cites repository metadata for the advertised OpenWork relationship (C27). C11 now explicitly says mutation receipts are optional.

## Evidence and remaining coverage

The dossier includes 41 complete representations: 35 original acquired source texts, five original raw HTML bodies alongside their visible-text normalizations, and one previously retained OpenWork metadata body. Full original raw body hashes remain unchanged. All current claim policies retain original acquisition times; S31–S35 have unknown observation freshness because transport metadata was not retained. A pinned URL establishes source identity, not an invented embedded version-binding passage.

Every question remains `needs_evidence`. Each vendor/source family has only one conservative independence group, below the requested two-group evidence floor. The original 12 queries and three complete aggregated returns are preserved in [discovery-supplement.json](discovery-supplement.json). Exact per-query result mappings and search timestamps cannot be reconstructed, so both reconciled formal search ledgers are intentionally empty. This creates explicit neutral/challenge search and included-source ledger gaps rather than fabricated coverage. The original generic candidate screening is not exhaustive screening, and versioned challenge issue dispositions remain open.

| Question | Additional substantive gaps |
|---|---|
| Q1 Codex | Effective default residency configuration, sender-driven caller authorization, executable recovery acceptance; S31–S32 freshness unknown. |
| Q2 OpenCode | Actual background registry persistence, reused-child and permission derivation paths, enforced hard budgets; S34–S35 freshness unknown. |
| Q3 Orca | Process readiness, capability mint/revocation, remote admission/recovery and executed races. |
| Q4 Claude | Full local/cloud schedule taxonomy conflict; actual version/account/runtime behavior remains untested. |
| Q5 OpenAI | Retirement/legacy body conflict, live account availability and Codex Web route status. |
| Q6 OpenWork | Executable dependency/enforcement paths, file-level license inventory and Enterprise Features subscription terms. |

G02 complete inventory and G19 matched operational comparison remain outside this bounded lane. Proposed Helm requirements must be reconciled with the independent local audit; they are not findings that Helm lacks those features. The prior acquisition-dossier review cannot accept this new dossier; a fresh independently bound question-level review is required. No reviewer callback or self-admission was invoked.

See [conversion-receipt.json](conversion-receipt.json) for hashes and the bounded packet-size adjustment. The kernel calls its serialized-byte upper bound a token estimate; the 250000 ceiling is artifact capacity, not model usage or paid-provider authorization.
