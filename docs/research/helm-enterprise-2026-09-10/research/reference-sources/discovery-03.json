"orca/skill-guides/orchestration.md at main · stablyai/orca · GitHub (https://github.com/stablyai/orca/blob/main/skill-guides/orchestration.md)\nciteturn98search0 [wordlim: 200] Crawled: last month; If a task says to use Orca orchestration, the coordinator must create or bind a Run, create the Task with `orca orchestration task-create`, then attach the worker with either the preferred `orca orchestration worker-start` composition or the low-level `orca orchestration dispatch --inject` path. ... Process every message before acknowledging; `check --ack <id> --wait` acknowledges, checks, and waits in one operation. ...   * It remains `outcome_unknown`: either `worker-stop --dispatch <id>` and inspect again, or explicitly `worker-abandon --dispatch <id>` while accepting that resources may still be live.\nUse Computer Use for browser windows, webviews, Orca app UI, or desktop UI outside Orca's embedded browser.\n\n# Orca Inter-Agent Orchestration\n\nOrchestration is Orca's structured coordination layer for agent messages, task ownership, dispatch state, and worker completion tracking.\n\nUse this skill when coordination state matters. For lightweight terminal prompts or basic worktree/terminal/built-in-browser control, use `orca-cli`.\n\n## Tool Boundary\n\nIf a task says to use Orca orchestration, the coordinator must create or bind a Run, create the Task with `orca orchestration task-create`, then attach the worker with either the preferred `orca orchestration worker-start` composition or the low-level `orca orchestration dispatch --inject` path.\n\nDo not substitute non-Orca subagent tools, generic agent-spawn APIs, or chat-only parallel worker features. Those may create useful workers, but they do not create Orca task/dispatch provenance, injected lifecycle preambles, `worker_done` authority, or decision gates.\n\nBefore claiming a worker was orchestrated, verify the task/dispatch exists:\n    \n    orca orchestration task-list --json\n    orca orchestration dispatch-show --task <task_id> --json\n\nIf the work was accidentally run outside Orca orchestration, say so plainly. To repair provenance, rerun or revalidate the needed work through a fresh Orca terminal plus injected dispatch; do not retroactively describe the external worker as orchestrated.\n\n## Tasks And Dispatch\n\nA Run is the namespace/inbox, a Task is the work item, and a Dispatch assigns one Task attempt to a terminal. Create or bind a Run once before the common loop.\n    \n    orca orchestration run-create --objective <text> --json\n    orca orchestration task-create --spec <text> [--deps <json_array>] [--parent <task_id>] [--json]\n    orca orchestration task-list [--status <status>] [--ready] [--brief] [--json]\n    orca orchestration task-update --id <task_id> --status <status> [--result <json>] [--json]\n    orca orchestration dispatch --task <task_id> --to <handle> [--from <handle>] [--inject] [--json]\n    orca orchestration dispatch-show --task <task_id> [--json]\n\nTask statuses: `pending`, `ready`, `dispatched`, `completed`, `failed`, `blocked`.\n\nDispatch rules:\n\n  * `--inject` sends the task spec plus preamble into a recognized agent CLI so it can report `worker_done`.\n  * If the target is a bare shell, omit `--inject`, dispatch for tracking if needed, then send the prompt manually with `orca terminal send --terminal <handle> --text <prompt> --enter --json`.\n  * After 3 consecutive failures on one task, the dispatch context circuit-breaks and the task is marked failed.\n  * Use `task-list --brief --json` for coordinator sweeps; it collapses whitespace and caps each echoed spec at 160 characters (`spec_truncated` marks shortened rows). Omit `--brief` when the full spec is required, or when an older CLI rejects it as an unknown flag.\n\n## Preferred Supervised Worker Loop\n\nUse `worker-start` for the normal supervised path. It composes the existing worktree, terminal, readiness, and dispatch primitives while returning exact created/reused effects. Agents still choose placement and concurrency; Orca does not schedule workers or infer conflicts.\n\nCreate the Run and every independent Task first, then start all independent workers before waiting:\n    \n    orca orchestration run-create --objective \"<objective>\" --json\n    orca orchestration task-create --spec \"<worker A task>\" --json\n    orca orchestration task-create --spec \"<worker B task>\" --json\n    orca orchestration worker-start --task <task_a> --worktree current --agent codex --json\n    orca orchestration worker-start --task <task_b> --worktree current --agent claude --json\n\n`current` and exact existing worktrees create a fresh agent terminal and do not rerun setup. Reuse an existing agent only with `--terminal <handle>`.\n\nFor a new worktree, setup runs by default and agent-first creation reuses the returned startup agent terminal:\n    \n    orca orchestration worker-start --task <task_id> --worktree new-child --name <name> --agent codex --setup run --json\n    # Independent/top-level:\n    orca orchestration worker-start --task <task_id> --worktree new-top-level --name <name> --agent codex --setup run --json\n\nSetup normally starts alongside the agent. Only a repository explicitly configured with `wait-for-setup` delays agent launch until setup succeeds. Use `--setup skip` or `--setup inherit` only for a concrete reason.\n\nRead the returned receipt before continuing: `ready` plus setup `running` is normal for start-immediately, while wait-for-setup returns setup `succeeded` before accepting task input. A failed or unknown start exits nonzero; inspect its `stage`, `effects`, and `residualResources` instead of guessing or automatically retrying. A wait-for-setup timeout can honestly leave setup `running`, which is not proof of failure.\n\nTo run the worker on another connected Orca server, add `--on <saved-environment>`. The Run and Tasks remain authoritative on the current server; later commands route by Dispatch ID, so never repeat `--on`:\n    \n    # Mac Run home -> Windows worker (the reverse is identical from a Windows Run home)\n    orca orchestration worker-start --task <task_id> --on windows --worktree new-top-level --repo <exact_remote_repo_selector> --name <name> --agent codex --setup run --json\n    orca orchestration worker-show --dispatch <dispatch_id> --json\n    orca orchestration worker-read --dispatch <dispatch_id> --limit 50 --json\n    orca orchestration send --to dispatch:<dispatch_id> --subject \"Follow-up\" --body \"<attempt-specific guidance>\" --json\n\nRemote `current` and `new-child` are intentionally invalid because those words are ambiguous across servers.     orca orchestration check --ack <delivery_id> --wait --types worker_done,escalation,question --timeout-ms 900000 --json\n\nWorkers report exactly once using the IDs and capability injected by Orca; they do not supply Run/server/terminal identity:\n    \n    orca orchestration send --type worker_done --subject \"<status>\" --body \"<what changed, findings, and what remains>\" --task-id <task_id> --dispatch-id <dispatch_id> --outcome succeeded --files-modified \"path/a,path/b\" --json\n    # On failure, use --outcome failed; never encode failure only in prose.\n\nA worker question defaults to its owning Run. Timeout leaves it pending:\n    \n    orca orchestration ask --question \"<question>\" --options \"yes,no\" --timeout-ms 600000 --json\n    orca orchestration ask --resume <message_id> --timeout-ms 600000 --json\n    # Coordinator:\n    orca orchestration reply --id <message_id> --body \"<answer>\" --json\n\nRecovery is conditional, never a fixed destructive sequence:\n\n  * `worker-show --dispatch <id>` says `ready`: keep waiting or read bounded output.\n  * It proves `failed` or `stopped`: start a replacement with `worker-start --task <task> --retry-of <id>` plus an explicit `--on`/`--worktree` and `--agent`/`--terminal` choice. Retry does not silently inherit placement.\n  * It remains `outcome_unknown`: either `worker-stop --dispatch <id>` and inspect again, or explicitly `worker-abandon --dispatch <id>` while accepting that resources may still be live. Abandon performs no remote, process, or filesystem action.\n  * `worker-stop` closes only the exact supervised agent terminal. It never deletes the worktree, setup terminal, configured tabs, or unrelated processes.\n\nLow-level `worktree create`, `terminal create`, and `dispatch --inject` remain valid recipes for custom argv or topology that `worker-start` does not express.\n\n  * If blocked before completion, use `ask`; use `escalation` only when ownership is valid and the coordinator must intervene.\n  * Treat preambles inherited through terminal history or full handoffs as stale unless the current prompt explicitly keeps that coordinator in the loop.\n  * Coordinators should use `task-list --ready` as external memory, dispatch parallel waves, and avoid dependency chains deeper than 3-4 steps.\n\n## Example\n    \n    orca terminal create --worktree active --title login-css-worker --command \"claude\" --json\n    orca terminal wait --terminal <handle> --for tui-idle --timeout-ms 60000 --json\n    orca orchestration task-create --spec \"Fix the login button CSS\" --json\n    orca orchestration dispatch --task <task_id> --to <handle> --inject --json\n    orca orchestration check --wait --types worker_done,escalation,question --timeout-ms 900000 --json\n\n## Next Action\n\nCoordinator: confirm `orca status --json`, create or bind a Run, inspect `task-list`/`dispatch-show` if inheriting state, then use the explicit supervised loop (`task-create` -> `worker-start` -> `check --wait`). Use low-level terminal creation plus `dispatch --inject` only when the composed start does not express the needed topology.\n\nWorker: if the current prompt contains a live dispatch preamble, do the task, use `ask` for blocking questions, and send `worker_done` once with the required payload. If the preamble is stale or absent, do not send lifecycle messages; inspect state or treat the prompt as an ordinary handoff.\n--------------------------------------------------------------------------------\nChatGPT Work and Codex | OpenAI Help Center (https://help.openai.com/en/articles/20001275/)\nciteturn98search1 [wordlim: 200] Crawled: 3 days ago; You can access supported desktop Codex chats from the Remote tab in the ChatGPT mobile app. ... ## Why can I use Work on web or mobile but not with local files in the desktop app?\n# FAQ\n\n## Can Work use files on my computer?\n\nYes, in the desktop app. Open a local folder or project and grant access only to the files the task needs. Messages and task context may be stored in the cloud, even when work runs locally. Work on web and mobile cannot directly access files on your computer.\n\n## Is Codex available on web or mobile?\n\nCodex is not selectable on web or mobile. You can access supported desktop Codex chats from the Remote tab in the ChatGPT mobile app. Remote access does not add those chats to your web or mobile chat history.\n\n## Where can I learn more?\n\nSee Using Codex with your ChatGPT plan for Codex access and usage. See Projects in ChatGPT for help organizing ongoing work, and Scheduled Tasks in ChatGPT for recurring or trigger-based work.\n\n## How do you switch between Chat, Work, and Codex?\n\nOn desktop:\nSelect ChatGPT or Codex from the top-left menu. In ChatGPT, select Chat or Work from the toggle at the top of the page. Chat and Work chats appear together in Recents. Use the filter to show all chats or only Chat or Work, and sort or pin chats as needed. Codex remains a separate view; its workflows are unchanged, and its history remains separate from ChatGPT history.\nWhile you’re in Codex, select New chat to open Quick Chat.\nOn mobile:\nSelect Chat or Work from the dropdown at the top of the screen. Codex is not selectable on mobile, but you can access supported desktop Codex chats from the Remote tab.\n\n## Can I use Voice with Work or Codex?\n\nYes, for eligible accounts in the ChatGPT desktop app on macOS and Windows. Voice uses the tools and permissions available to the selected experience. Work on web and mobile remains separate, and Codex is not selectable on web or mobile.\n\n## What’s happening to regular ChatGPT?\n\nThe fast, familiar Chat experience is still available.\nOn desktop, select ChatGPT from the top-left menu, then select Chat from the toggle at the top of the page.\nOn mobile, select Chat from the dropdown at the top of the screen.\n\n## Why can I use Work on web or mobile but not with local files in the desktop app?\n\nAs we roll out the Work feature, Cloud Work and local Work access may update at different times. First, ask a workspace owner or admin to confirm the Work permission assigned to your role. If the permission is enabled, allow time for the local-permission backfill to complete before contacting Support.\n--------------------------------------------------------------------------------\norca/src/main/runtime/rpc/methods/orchestration-worker-control.ts at main · stablyai/orca · GitHub (https://github.com/stablyai/orca/blob/main/src/main/runtime/rpc/methods/orchestration-worker-control.ts)\nciteturn98search2 [wordlim: 200] Crawled: 4 weeks ago; } else if (['ready', 'failed', 'stopped', 'start_unknown'].includes(attachment.state)) { ... const abandoned = runtime.getOrchestrationDb().abandonWorkerDispatch(params.dispatch)\nparams: WorkerDispatchParams,\n\nhandler: async (params, { runtime }) => {\n\nconst db = runtime.getOrchestrationDb()\n\nconst dispatch = db.getDispatchContextById(params.dispatch)\n\nlet worker = db.getWorkerDispatch(params.dispatch)\n\nif (!dispatch || !worker) {\n\nthrow new OrchestrationError(\n\n'dispatch_not_found',\n\n`Worker Dispatch ${params.dispatch} was not found.`\n\n)\n\n}\n\nconst federated = db.getFederatedDispatch(params.dispatch)\n\nif (federated) {\n\nconst server = resolvePinnedFederatedServer(runtime, federated)\n\nruntime.ensureOrchestrationFederationRelay(dispatch.run_id)\n\nconst remote = await callFederatedWorkerShow(runtime, federated)\n\nconst attachment = remote.attachment\n\nworker = db.updateWorkerSetupEvidence({\n\ndispatchId: params.dispatch,\n\nsetupState: attachment.setup_state,\n\neffects: attachment.effects\n\n}).worker\n\nif (\n\nattachment.state === 'succeeded' ||\n\n(attachment.state === 'failed' && attachment.stage === 'worker_report_queued')\n\n) {\n\nawait runtime\n\n.syncOrchestrationFederatedDispatchAfterCurrent(params.dispatch)\n\n.catch(() => undefined)\n\n} else if (\n\nattachment.state === 'stopped' &&\n\n['stopping', 'stop_unknown'].includes(worker.state)\n\n) {\n\nworker = db.reconcileFederatedWorkerStop(params.dispatch)\n\n} else if (['ready', 'failed', 'stopped', 'start_unknown'].includes(attachment.state)) {\n\nworker = db.reconcileFederatedWorkerStart({\n\ndispatchId: params.dispatch,\n\nstate: attachment.state as 'ready' | 'failed' | 'stopped' | 'start_unknown',\n\nstage: attachment.stage,\n\nlastError: attachment.last_error,\n\nworktreeId: attachment.worktree_id,\n\nterminalHandle: attachment.terminal_handle,\n\nsetupState: attachment.setup_state,\n\neffects: attachment.effects,\n\nresidualResources: attachment.residualResources\n\n})\n\nif (\n\nattachment.state === 'ready' &&\n\nattachment.worktree_id &&\n\nattachment.terminal_handle\n\n) {\n\ndb.updateFederatedDispatchResources({\n\ndispatchId: params.dispatch,\n\nremoteRuntimeEpoch: remote.runtimeEpoch,\n\nworktreeId: attachment.worktree_id,\n\nterminalHandle: attachment.terminal_handle\n\n})\n\n}\n\n}\n\nworker = db.getWorkerDispatch(params.dispatch)\n\nif (!worker) {\n\nthrow new OrchestrationError(\n\n'dispatch_not_found',\n\n`Worker Dispatch ${params.dispatch} was not found after remote reconciliation.`\n\n)\n\n}\n\nreturn {\n\ndispatch: db.getDispatchContextById(params.dispatch),\n\nworker: exposeWorker(worker),\n\nserver: { environmentId: server.environmentId, name: server.name },\n\n--------------------------------------------------------------------------------\nsubagent: background subagents hang silently on repeated upstream 429s; no error or completion notification surfaces · Issue #44931 · anomalyco/opencode · GitHub (https://github.com/anomalyco/opencode/issues/44931)\nciteturn98search3 [wordlim: 200] Published: 2 weeks ago; Crawled: last week; The only way to discover the state is to interrupt the whole session, after which the subagent finally surfaces as `cancelled`. ...   * A background subagent whose upstream requests fail should surface that failure promptly: a terminal error/part result event to the parent session plus a visible indicator in the TUI. ...   * Happy to pull the exact server-log lines (`role=server`, child session run ids) from `~/.local/share/opencode/log/opencode.log` for the affected timeframe if useful — say the word and I'll attach a redacted excerpt.\n\n# subagent: background subagents hang silently on repeated upstream 429s; no error or completion notification surfaces #44931\n\nNew issue\n\nCopy link\n\nNew issue\n\nCopy link\n\nOpen\n\nOpen\n\nsubagent: background subagents hang silently on repeated upstream 429s; no error or completion notification surfaces#44931\n\nCopy link\n\nAssignees\n\nLabels\n\n2.0\n\n## Description\n\ngarysassano\n\nopened on Aug 25, 2026\n\nIssue body actions\n\n## Summary\n\nA `subagent` tool call launched with `background: true` never reports anything when its child-session model requests start failing (repeated upstream HTTP 429 from a rate-limited provider subscription). The parent session keeps running normally, the promised automatic \"finished\" notification never arrives, and the TUI shows nothing wrong. The only way to discover the state is to interrupt the whole session, after which the subagent finally surfaces as `cancelled`. Meanwhile retries keep burning the already-exhausted quota.\n\n## Environment\n\n  * opencode version: 0.0.0-next-17403\n  5. Press Esc / interrupt the session.\n  6. Only now does a notice appear that the subagent ended, with state `cancelled`.\n\n## Expected Behavior\n\n  * A background subagent whose upstream requests fail should surface that failure promptly: a terminal error/part result event to the parent session plus a visible indicator in the TUI.\n  * The automatic \"you will be notified when it finishes\" contract should also hold on the failure path, not only on success.\n  * Ideally the runner applies bounded retry/backoff for transient 429s and then reports a distinguishable terminal state (e.g. `failed: rate limited`) instead of leaving the child in an indefinite in-progress limbo.\n\n## Actual Behavior\n\n  * The child session stalls indefinitely; no event, notification, or TUI change indicates a problem.\n  * The parent agent cannot answer questions about subagent progress (\"what's the subagent doing?\" got no useful answer for many minutes).\n  * Retries against the rate-limited provider continue silently, consuming more of the exhausted quota.\n  * After a manual interrupt, the subagent is reported only as `cancelled` (session id prefix `ses_fc8036a7…` in that run; parent `ses_fc817cb3…`). Nothing in the visible UI hinted at the 429 cause; it had to be inferred from the provider dashboard.\n\n## Additional Context\n\n  * Frequency: reproduced consistently within one long session whenever the upstream subscription was rate-limited; two different routed providers exhibited the same silent-hang behavior, which is why it looks like a runner-level gap rather than a provider adapter bug.\n  * Workaround: avoid `background: true` for subagents while on a rate-limited plan and run exploration inline in the primary agent, or interrupt the session to reclaim control and re-dispatch.\n  * Happy to pull the exact server-log lines (`role=server`, child session run ids) from `~/.local/share/opencode/log/opencode.log` for the affected timeframe if useful — say the word and I'll attach a redacted excerpt.--------------------------------------------------------------------------------\n[Bug]: worker-abandon/worker-stop return dispatch_not_found for a live dispatch dispatch-show returns (1.4.175) · Issue #13005 · stablyai/orca · GitHub (https://github.com/stablyai/orca/issues/13005)\nciteturn98search4 [wordlim: 200] Published: last month; Crawled: 3 weeks ago; > It remains `outcome_unknown`: either `worker-stop --dispatch <id>` and inspect again, or ...     $ orca orchestration worker-abandon --dispatch ctx_b4a0b03319b2 --json\nThe positive control below is new and was run\non 1.4.175. Note `appVersion` is no longer exposed in the CLI's `_meta` envelope on this build —\nthe version above is from the app bundle's `CFBundleShortVersionString`.\n\n## Summary\n\nA dispatch created with `orca orchestration dispatch` cannot be released. Three commands\ndisagree about whether the same dispatch id exists, in the same runtime, seconds apart:\n\nCommand  | Answer about `ctx_b4a0b03319b2`\n--- | ---\n`orchestration dispatch-show --task <t>`  | exists, `\"status\": \"dispatched\"`\n`orchestration dispatch --to <same terminal>`  | exists — refuses with \"already has an active dispatch (ctx_b4a0b03319b2 …)\"\n`orchestration worker-abandon --dispatch ctx_b4a0b03319b2`  | `dispatch_not_found`\n`orchestration worker-stop --dispatch ctx_b4a0b03319b2`  | `dispatch_not_found`\n\nThe net effect is a permanently wedged terminal: it holds an active dispatch that blocks any\nfurther dispatch to that pane, and both documented release verbs deny the dispatch exists.\n\nThis matters because the orchestration guide prescribes exactly these two verbs as the escape\nhatch for an unresolved worker:\n\n> It remains `outcome_unknown`: either `worker-stop --dispatch <id>` and inspect again, or\n> explicitly `worker-abandon --dispatch <id>` while accepting that resources may still be live.\n\nBoth halves of that instruction fail with `dispatch_not_found`.\n\n## Cause — measured, via the positive control\n\nBoth failing dispatches were created by the low-level `orchestration dispatch` path, so they carry\n`launch_token_hash: null`, `capability_hash: null`, `process_incarnation: null`.\n\nThe positive control has now been run, and it confirms the scoping. The same verb against a\n`worker-start`-created dispatch, on the same runtime minutes later:\n    \n    $ orca orchestration worker-start --task <task> --agent codex --worktree current --json   # ok:true\n    $ orca orchestration dispatch-show --task <task> --json\n        → {\"id\":\"ctx_0f6f82769296\",\"status\":\"dispatched\",\n           \"capability_hash\":\"030330260603acb0…\",           # ← populated\n           \"process_incarnation\":\"47c13b8f-…\",              # ← populated\n           \"assignee_pane_key\":\"5d4ca30d…:eb4923a5…\"}       # ← populated\n    \n    $ orca orchestration worker-abandon --dispatch ctx_0f6f82769296 --json\n    {\"ok\":true,\"result\":{\"dispatchId\":\"ctx_0f6f82769296\",\"state\":\"abandoned\",\n      \"alreadySettled\":false,\"stale\":false,\"processAction\":\"none\", …}}          # ← works\n--------------------------------------------------------------------------------\nworker-start reports agent_readiness timeout as failed while the launched agent keeps running · Issue #13653 · stablyai/orca · GitHub (https://github.com/stablyai/orca/issues/13653)\nciteturn98search5 [wordlim: 200] Published: last month; Crawled: 2 weeks ago; When `orca orchestration worker-start` times out at the `agent_readiness` stage, the daemon declares the dispatch `failed`, fails the task, and revokes the dispatch capability — while the agent it just launched keeps running. ... That function returns `true` only for an error carrying `code: 'operation_unknown'`, or for stage `worktree_create` (`src/main/runtime/rpc/methods/orchestration-worker-topology.ts:269`).\n\n# worker-start reports agent_readiness timeout as failed while the launched agent keeps running #13653\n\nNew issue\n\nCopy link\n\nNew issue\n\nCopy link\n\nOpen\n\nOpen\n\nworker-start reports agent_readiness timeout as failed while the launched agent keeps running#13653\n\nCopy link\n\nAssignees\n\n## Description\n\nartile\n\nopened on Aug 10, 2026\n\nIssue body actions\n\n## Summary\n\nWhen `orca orchestration worker-start` times out at the `agent_readiness` stage, the daemon declares the dispatch `failed`, fails the task, and revokes the dispatch capability — while the agent it just launched keeps running. The lane becomes a live agent that nobody supervises and a dispatch that nobody can report to.\n\nA readiness handshake that times out proves that the handshake did not finish. It does not prove that nothing started. Today the two are treated as the same thing.\n\n## What we observed\n\nOrca v1.4.176, 2026-08-10, macOS, one host under heavy load (1-minute load average above 200).\n\nSeven lanes were dispatched from one coordinator:\n## What the code does\n\nTraced on `upstream/main` at `c0c893d171` (package version 1.4.178-rc.2). Every step below is present there.\n\n  1. A `tui-idle` wait rejects on timeout. `src/main/runtime/orca-runtime.ts:17236` (live-pty path) and `src/main/runtime/orca-runtime.ts:17334` (leaf path) both do `reject(new Error('timeout'))`. That literal string is the `last_error: \"timeout\"` we saw.\n  2. `src/main/runtime/rpc/methods/orchestration-workers.ts:196` sets `failedStage = 'agent_readiness'` and awaits that wait. The rejection skips the `!wait.satisfied` branch entirely and lands in the handler's `catch`.\n  3. `failWorkerStartWithReceipt` classifies the error with `isUnknownWorkerStartOutcome` (`src/main/runtime/rpc/methods/orchestration-worker-start-receipt.ts:19`). That function returns `true` only for an error carrying `code: 'operation_unknown'`, or for stage `worktree_create` (`src/main/runtime/rpc/methods/orchestration-worker-topology.ts:269`). A readiness timeout matches neither, so it takes the `failed` path.\n\nThe existing `outcome_unknown` state does not close this second gap by itself. `markWorkerStartUnknown` (`src/main/runtime/orchestration/db.ts:4365`) also sets `capability_revoked_at` and leaves the dispatch in `pending`, never `dispatched`, so a late `worker_done` is still rejected. What `outcome_unknown` does give is an honest label, a task left `blocked` rather than `failed`, and the documented recovery in the orchestration skill: \"either `worker-stop --dispatch <id>` and inspect again, or explicitly `worker-abandon --dispatch <id>` while accepting that resources may still be live.\"\n\n## What a caller can and cannot conclude from `failed`\n\nToday, `state: failed` with `failedStage: agent_readiness` means only this: no readiness signal arrived within `timeoutMs`.\n\nIt does not mean:\n\n  * that no worktree was created — one was, and it is listed in `residual_resources`;\n  * that no terminal was created — one was;\n  * that no agent process is running — one usually is, and on a loaded machine it is the common case;\n  * that the resources are safe to reuse — a retry into the same worktree stacks a second agent terminal on it;\n  * that the task is finished in any sense, although `tasks.status` is now `failed`.\n\nA coordinator that reads `failed` as \"nothing happened, start again\" is wrong on every one of those points, and the API gives it no way to tell the difference between a launch that died and a launch that is merely late.\n\n--------------------------------------------------------------------------------\n[Feature]: worker-start --spec — create the task and dispatch it in one call · Issue #13360 · stablyai/orca · GitHub (https://github.com/stablyai/orca/issues/13360)\nciteturn98search6 [wordlim: 200] Published: last month; Crawled: yesterday; # [Feature]: worker-start --spec — create the task and dispatch it in one call #13360 ... The brief lives in a separate `orca orchestration task-create --spec \"<text>\"`, whose only output an orchestrator wants is the `task_id` it immediately passes to `worker-start`. ...   * `--retry-request` should cover the composite the way it covers the current call, so an unknown-result retry cannot double-create the task. ... to join this conversation on GitHub.\n\n# [Feature]: worker-start --spec — create the task and dispatch it in one call #13360\n\nNew issue\n\nCopy link\n\nNew issue\n\nCopy link\n\nOpen\n\n#13540\n\nOpen\n\n[Feature]: worker-start --spec — create the task and dispatch it in one call#13360\n\n\n## Description\n\nartile\n\nopened on Aug 9, 2026\n\nIssue body actions\n\n### Problem or use case\n\nSpawning one supervised worker takes two mutations today, and the split gets awkward exactly where the CLI is otherwise at its best.\n\n`orca orchestration worker-start` already carries almost the whole spawn decision in one call: whether to make a worktree at all and which kind (`--worktree current | new-child | new-top-level | <selector>`), plus `--name`, `--repo`, `--base-branch`, `--display-name`, `--comment`, `--setup`, and the harness triple `--agent` / `--model` / `--effort`. That is genuinely good — one command, one receipt, one record shape.\n\nThe one thing it cannot carry is the work itself. The brief lives in a separate `orca orchestration task-create --spec \"<text>\"`, whose only output an orchestrator wants is the `task_id` it immediately passes to `worker-start`. So the shape is always:\n    \n    TASK=$(orca orchestration task-create --run \"$RUN\" \\\n      --task-title \"COSM-309 — wave tree builder\" \\\n      --spec \"COSM-309\" --json | jq -r '.result.task.id')\n    \n    orca orchestration worker-start --run \"$RUN\" --task \"$TASK\" \\\n      --worktree new-child --name cosm-309-wave-tree --base-branch local \\\n      --agent claude --model claude-sonnet-5 --effort medium\n\nTwo mutations, one of them a pure id-shuttle. It is not fatal, but it has real costs:\n\n  * It is not atomic. If `task-create` succeeds and `worker-start` fails, an orphan task is left `ready` and never dispatched. That happened during our run — `task_ecf32e44143d` was created twice and the first copy is still sitting `ready`, dispatched to nobody. Nothing in `task-list` distinguishes \"waiting for a worker\" from \"abandoned by a failed spawn\".\n  * It forces shell plumbing into what is otherwise a declarative call. Every orchestrator ends up writing the same `jq -r '.result.task.id'` dance, and an agent driving the CLI has to hold the intermediate id across two tool calls.\n--------------------------------------------------------------------------------\n[Other]: Tracking issues from the Orchestration loop · Issue #15190 · stablyai/orca · GitHub (https://github.com/stablyai/orca/issues/15190)\nciteturn98search7 [wordlim: 200] Published: 3 weeks ago; Crawled: last week; A scream is right for `worker-start`, where the alternative is recording a lie — but it discards the message id, dispatch id and warnings the same call produced, so a caller cannot tell \"not submitted, but the dispatch exists at id X\" from \"everything failed\". ...   * A Codex reviewer completed its work and could not deliver it — two `worker_done` attempts refused with \"Orca is not running\" while `orca status` from the coordinator reported `ready` and reachable.\nThe open question is whether refusing the mutation has to also remove the message from the coordinator's actionable set.\n\nEvidence from running the loop (measured 2026-08-22)\n\nThe four steps above were built by four supervised agent lanes reviewed by four independent ones. Running that produced measurements of the same failures this map tracks, which are recorded here because they are observations rather than argument.\n\n  * Every first `worker-start` failed at `dispatch_input` with `agent_prompt_stalled`, on both Claude and Codex, on a cold agent — twelve failed dispatches against ten that succeeded. Recovery that worked: one `terminal send --interrupt`, then `worker-start --retry-of <ctx> --terminal <handle> --worktree path:<...>` onto the now-warm agent.\nCorrection to an earlier reading of this. A first pass here attributed all of it to #14822 — the preamble arriving complete and never being submitted. A measured follow-up refuted that as the whole story: `agent_prompt_stalled` is also returned for prompts that were delivered and submitted, whenever the agent answers without starting a turn, because the verification accepts only a `workingSequence` transition. --------------------------------------------------------------------------------\nworker-start can paste a Codex dispatch before its TUI is ready; submit Enter is lost and worker idles · Issue #15581 · stablyai/orca · GitHub (https://github.com/stablyai/orca/issues/15581)\nciteturn98search8 [wordlim: 200] Published: 3 weeks ago; Crawled: 2 weeks ago; # DRAFT — upstream issue for stablyai/orca ... `orca orchestration worker-start` can bracket-paste the dispatch into Codex while the Codex TUI is still starting, then send Enter before the composer accepts submission.\n--------------------------------------------------------------------------------\nTask lifecycle: the tool misdescribes async work, results go missing, and stops leave work running · Issue #45480 · anomalyco/opencode · GitHub (https://github.com/anomalyco/opencode/issues/45480)\nciteturn98search9 [wordlim: 200] Published: 2 weeks ago; Crawled: last week; Some need `OPENCODE_EXPERIMENTAL_BACKGROUND_SUBAGENTS` and some do not, which changes who is affected. ... Cancel a nested task and its waiting caller is never told. ... Context overflow, output limits, cancellation, and a child that died without recording an error at all arrive looking the same, and long error text is either truncated to uselessness or dumped whole. fix(opencode): surface resumable subagent errors #43657 and fix(opencode): surface subagent tool errors #43821 recently fixed one arm of this: a child that errored now reads as a failure rather than an empty success.\n--------------------------------------------------------------------------------\ncodex/sdk/python/docs/getting-started.md at main · openai/codex · GitHub (https://github.com/openai/codex/blob/main/sdk/python/docs/getting-started.md)\nciteturn98search10 [wordlim: 200] Crawled: 2 weeks ago;         thread.run(\"Summarize Rust ownership in two bullets.\") ...         thread = codex.thread_resume(\"thr_123\") ...     from openai_codex import AsyncCodex, Sandbox\n--------------------------------------------------------------------------------\n[Feature]: First-class per-invocation model and effort for orchestration workers · Issue #12736 · stablyai/orca · GitHub (https://github.com/stablyai/orca/issues/12736)\nciteturn98search11 [wordlim: 200] Published: last month; Crawled: 2 weeks ago;   * `orca orchestration worker-start --agent codex` has no per-dispatch model or effort option. ... That workaround is functional, but it turns one typed operation into several agent-specific shell operations.\n"