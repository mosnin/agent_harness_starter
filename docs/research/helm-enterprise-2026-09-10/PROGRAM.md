# Hades / Helm enterprise program — revision 4

Requested and authorized on 2026-09-10. This is an active implementation and acceptance program, not a readiness declaration. The calling Codex goal is the durable objective. Every gate below must have a reproducible receipt before final acceptance. A blocked environment, unavailable account, mocked provider or source inspection is not a live pass.

## Baseline and ownership

- Hades: `mosnin/agent_harness_starter`, `claude/hermes-swarm-framework-vbhrot`, `81cd435977ee5f8b6e7e78f0595aa3da1777af78`.
- Helm: actual `mosnin/opencode` fork, `helm-integration`, `89bac15fcea8f7fb90d50c6d4cf457b25673a7b5` (local branding commit, not rebuilt); OpenCode 1.18.21 baseline, MIT notices retained. This program preserves its native coding workspace rather than replacing it with a lookalike.
- Orca: actual available source `stablyai/orca`, `bf4e2705046cf9ef9c915929a9646da85717af07`. Hades now has a source adapter; the actual runtime has not been packaged or executed. Orca must own its workers/worktrees when selected; Hades owns the objective, budget and acceptance. No two engines may own one attempt.
- Root: integration, durable Work scheduler, evidence and benchmark gates. Integration lane: actual Orca backend adapter and packaging contract. Experience lane: Helm branding and usable Work controls. Research lane: current primary-source evidence. Final review requires a fresh reviewer who did not produce the implementation or research claims being reviewed.

## Delivery order

1. Pin source baselines and replace product-facing OpenCode branding only in the Helm build. Preserve configuration, protocol and upstream provider identities and licensing.
2. Integrate Orca's actual Node runtime through a private, versioned, capability-negotiated boundary. Preserve the actual OpenCode server. Reconcile durable dispatch identities after uncertain outcomes; never resend a prompt merely because its acknowledgement was lost.
3. Extend existing Hades Work rather than creating a competing scheduler: rolling bounded task admission, declared edit reservations, per-task artifact gates, attempt history, durable budgets and clear review/recovery controls.
4. Study current Codex, Claude Code/Cowork and ChatGPT Work. Then inspect pinned OpenWork executable paths in depth, including its MIT/enterprise directory split. Map the complete documented product capabilities to existing, new, unavailable and unverified Hades behavior. Do not claim proprietary internal mechanisms are known.
5. Implement the remaining coding and general-work gaps from that matrix, including connector authority, durable execution, team controls, artifact/review workflows and operating limits. Candidate differentiators are evidence-bound delivery, recovery without duplicate effects, cross-provider teams and native/browser spatial review. Their advantage must be demonstrated, not declared a moat from feature count.
6. Build, exercise representative native workflows, run matched comparisons, fix findings, and admit independent review against the frozen candidate.

## Measurable acceptance gates

| Gate | Required result | Current evidence status |
| --- | --- | --- |
| G01 Identity | Zero unintended product-facing OpenCode labels/wordmarks in Helm mode; upstream mode and licenses preserved; source tests and built UI inspection  | Helm product branding committed locally at 89bac15f; 17 pure helper assertions pass. Built/native UI remains unverified. |
| G02 Reference coverage | Pinned, dated capability matrix covers every documented OpenCode/Orca and researched work-product capability, with a test or explicit remaining gap for each; fresh independent source review  | 79 documented catalog entries; initial/governed research and 43-file OpenWork extraction reviewed within explicit source gaps. Full capability acceptance is incomplete. |
| G03 Actual Orca | Clean-user, offline packaged orcad boot; source/artifact hashes, native PTY/watcher checks, negotiated capabilities; no dependency on developer checkout or unrelated Orca runtime  | Actual pinned Orca adapter and package recipe exist. Exact build/native dependencies absent; real orcad/PTY boot has not run. |
| G04 Isolated coding | At least eight frozen coding fixtures; correct changes, hidden oracle tests, no source mutation before integration, no unrelated paths changed  | Eight coding fixtures and hidden oracles frozen; broken/reference calibration passes. No real product coding trial in this program. |
| G05 Durable dispatch | Lost acknowledgement, crash at each dispatch boundary and restart produce exactly one accepted request/worker; absent proof remains unknown; repeat each fault at least three times  | Dispatch now pins source base/identity and repeated owned-worker observations; source import and durable apply claims pass fault fixtures, including an owned child SIGKILL. Real Orca runtime/restart trials remain open. |
| G06 Task graph | At least six dependency/parallelism fixtures; max concurrency never exceeded; conflicting declared edits never overlap across plans; eligible independent tasks continue when another task is slow  | Work source-operation reservations and task admission coordinate across shared-store processes; transaction, Stop, clean shutdown and dependent-release fixtures pass. External editors are outside these cooperative reservations. |
| G07 Evidence | Failed/missing/changed task outputs never release dependents or produce accepted goals; completion receipts include hashes and attempts; adversarial symlink/drift cases pass  | Explicit Orca task acceptance binds the import, applied review, fresh source checks and task file hashes. Stale source, pending instructions and foreign receipts refuse acceptance in real Workbench/Git fixtures. |
| G08 Authority | Profile/project mismatch, stale lease, child escalation, deny then late reply and expired approvals each cause zero unauthorized effects  | Profile/root/lease/child and cancelled queued-operation fixtures pass. Enterprise identity and native/provider authority trials remain open. |
| G09 Budget | Measured usage and unknown reservations remain distinct across restart; no automatic limit increase; at least four exhaustion/continuation cases  | Checked Orca output acceptance retains unknown provider reservations and original attempt history. Provider token/time/spend enforcement and replacement-worker retry semantics remain unimplemented. |
| G10 Cancellation | Cancel during provider, tool, approval and runtime reconnect stops new effects, preserves unknown outcomes and cannot accept a late result; owned resources only  | Queued Work-bound Helm mutations are revoked by Stop; source apply/checks recheck authority at dispatch. Independent Stop/close-during-fingerprint cases pass. Real provider cancellation remains unverified. |
| G11 Recovery | At least one real 60-minute multi-task run with process restart, resumed transcript, no replayed mutation and final checked artifacts; recovery timings recorded  | No real 60-minute provider run with process restart has been performed. |
| G12 Worktree integration | Dirty/untracked/binary/deleted-file fixtures, source drift and stale reviews fail safely; reviewed exact patch applies once and source checks pass  | Orca stopped output imports into a separate Helm review snapshot, followed by explicit verification, full-patch apply, fresh source checks and Work acceptance. Dirty/index/binary/deletion, drift, cross-process apply and known-applied recovery fixtures pass; actual packaged Orca/native workflow remains unverified. |
| G13 Provider execution | Real authorized Hades, embedded OpenCode/Helm, Claude and Codex workflows where available; executable detection alone never passes; unsupported engines remain visible  | No current real Hades/OpenCode/Orca/provider workflow acceptance. Native Hades control was denied; exact Orca and Helm build prerequisites are absent. |
| G14 General work | Frozen sourced-research, document, spreadsheet, connector and multi-step planning workflows produce independently checked artifacts; unavailable products/connectors remain gaps  | Seven native plugin source adapters and real framework instruction integration exist; expanded Scalar CRM and Cadre conversation reads are locally verified. All seven live OAuth journeys and real general-work comparisons remain open. |
| G15 Spatial product review | Native and browser capture → scoped review → coding handoff → changed UI → fresh before/after evidence, through real Hades  | Prior spatial implementation retained. No current native/browser capture-to-code round trip was executed. |
| G16 Product usability | Native keyboard and pointer workflows for launch, task, team, approvals, inspect, stop/resume and review; no dead ends, clipped content, misleading status or unbranded product surfaces  | Work and Orca link to existing Helm review UI with scoped explicit acceptance, stale-state invalidation and confirmed-application recovery controls. 102 focused DOM/RPC cases pass; installed native visual and interaction acceptance remains open. |
| G17 Enterprise controls | Authenticated user/organization/project boundaries, scoped connector credentials, durable audit/export/retention, policy revocation and operational backup/restore tests  | Atomic audit export/current-state binding and existing staged backup tests pass. Authenticated organization/RBAC/SSO/credential policy/retention remains incomplete. |
| G18 Regression | Clean source checks, relevant package tests/builds, all new negative fixtures; environment failures reported separately and rerun on a permitted host before acceptance  | Final Work/Helm offline gate296/296, Plugins254/254, full TypeScript and desktop source bundles pass. Runners share11 routing cases (539 distinct cases,46 files). Full native/runtime/provider regression remains open. |
| G19 Matched comparison | At least 24 frozen cases with three trials per available comparable harness, same task/model/budget where supported; report pass rates, elapsed time, usage, human interventions and recovery; no cherry-picking  | 24-case benchmark manifest and three-trial requirement frozen. Separate comparison task creation was denied; no real matched benchmark has run. |
| G20 Independent acceptance | Fresh reviewer reproduces decisive receipts, challenges safety/recovery/coverage and resolves material findings on exact candidate SHAs  | Independent provenance, apply, Work and source-check reviews repaired destination, cancellation, shutdown, transaction and completed-claim recovery defects. Final exact files are receipt-bound; product-wide independent acceptance remains open. |

## Evidence rules and current external constraints

Receipts record candidate revision, command, timestamps, environment, fixture/oracle hashes, result and limitations. Benchmark expected outputs are separate from prompts. Synthetic fault fixtures verify control logic, not provider intelligence. A product-wide superiority claim requires matched evidence and a stated scope; undocumented proprietary behavior stays unknown.

The requested separate Codex comparison task was attempted on 2026-09-10. Creation returned `MCP tool call requires approval, but approval policy is never`; no separate task exists. This is an open G19 execution gate, not permission to bypass review through another tool. Root network-listener tests are also restricted. Source work, deterministic local tests, primary-source research and permitted native workflows continue independently.

Disk headroom at baseline is below 1 GiB. Build staging must preserve installed/current candidate apps and user data; only identified task-generated regenerable outputs may be cleaned. Local source and mocked/fixture results do not establish installed-app readiness.

## Current source checkpoint

The goal remains active. Foundation source is committed locally at `ab74827cbe01181acfa1ec83ae54775b827f184e`; Helm branding is committed locally at `89bac15fcea8f7fb90d50c6d4cf457b25673a7b5`. Neither commit has been pushed or installed. Fixture verification above is deliberately narrower than installed product acceptance. The current [capability catalog](CAPABILITIES.md), [offline regression v2](audit/inprocess-regression-v2.md), [lifecycle review](review/desktop-lifecycle-repair-review.md), [OpenWork v2 review](review/openwork-architecture-v2-review.md), and machine-readable `STATUS.json` track the next gates. The initial regression has a preserved listener-fixture erratum; use v2 for explicitly offline fixtures.

Current environmental gates: Hades computer use was not approved; separate comparison-task creation required approval under a never-approve policy; local listeners return EPERM; Bun 1.3.14 and exact Orca dependencies are absent; disk headroom remains below 1 GiB. No alternative route was used to operate a denied application or listener. The UI/sidecar build is not a native app or actual fork rebuild.

## Follow-up source checkpoint: Plugins and Work

The prior Hades commit is `b6ec8b860252b34eefc80e0914bc1d1393eefb6a`; the follow-up candidate is pinned by [the v3 receipt](../../plugins/source-v3-receipt.json). It includes explicit durable Work dispatch to Orca, independently repaired admission fairness, fresh scoped plugin reads, exact write acknowledgements across seven adapters, expanded Scalar CRM and Cadre conversation projections. Current [source acceptance](../../plugins/ACCEPTANCE_V3.md) records 247 Plugins and 127 Work/Orca in-process tests, types and source bundles. These are distinct from actual runtime/provider/native acceptance.

The historical source checkpoint above and its counts remain evidence of that earlier candidate. The current goal and [Plugins program](../../plugins/PROGRAM.md) remain active, with 0/7 live account journeys, no packaged Orca execution, no real 60-minute team run, and no matched harness comparison accepted.

## Orca output handoff checkpoint

Starting from local Hades commit `c44644192872ff9e0887e38c9a68df1e910dc376`, the [Orca output report](audit/orca-output-v1.md) and [exact receipt](audit/orca-output-v1-receipt.json) record stopped-worker provenance, isolated Helm import, guarded source application/checks, explicit Work acceptance and confirmed-application recovery. The final offline gates pass 296 Work/Helm and 254 Plugins cases with 11 shared routing cases. Source bundles and TypeScript pass; installed/native/runtime/provider/product acceptance remains open. Earlier checkpoints above remain historical evidence.
