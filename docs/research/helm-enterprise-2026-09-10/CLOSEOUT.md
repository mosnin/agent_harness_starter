# Release closeout

The user asked to begin wrapping up after the Mac shut down. Keep the requested features and the existing acceptance gates; stop adding new feature ideas or exploratory research unless a concrete release defect requires it. Prioritize delivery, fixes and final acceptance. The full enterprise goal remains active.

## Recovered checkpoint

Hades source is saved at `1a429bf75918c4379961c21906b8472064ec7c30` in `mosnin/agent_harness_starter`, branch `claude/hermes-swarm-framework-vbhrot`. The worktree was clean before this report. All 113 recorded source/evidence files, the previous checkpoint receipt and four desktop bundle hashes match. All 109 receipt-bound provider files across the seven products also match. Glove and Company OS retain additional uncommitted work; it was not modified. Stored is still a retained patch.

The last verified regression is 683 distinct local source tests across 57 files, plus TypeScript and desktop source bundles. Those tests were not rerun after the shutdown. The saved bytes were checked instead. The newest Hades candidate has not been pushed or installed, and no live plugin account journey is accepted. See [the recovery snapshot](audit/restart-closeout-v1.json) and [the implementation report](audit/orca-replacement-v1.md).

## Closeout order

1. **Build and deliver the current product.** Reconcile the actual Hades, Helm/OpenCode, Orca, Browser and Maus revisions; package the pinned dependencies and install the exact candidate. Resolve the recorded disk/runtime prerequisites using only authorized work. The user excluded the Apple Developer account step for now; it is not a prerequisite for this local release. Native app control, remote writes and listeners were previously denied and cannot be bypassed.
2. **Deliver the seven account integrations.** Finish selective source delivery, Stored patch integration, required provider migrations and OAuth registration. In installed Hades, pass consent/callback, browse, agent read, conditional write, sync, refresh, revoke and reconnect for each account. Current accepted count: 0/7. Publish/verify the actual Company OS package and test update/rollback with the profile toggle.
3. **Close the existing runtime and enterprise gaps.** Implement and validate provider usage accounting and supported mandatory limits, and finish authenticated organization/role/policy boundaries. Preserve unknown usage and unsupported capabilities explicitly. These are remaining implementation work, not merely UI polish.
4. **Run final acceptance and fix failures.** Exercise the default coding-to-reviewed-change flow, native Browser/Maus spatial handoff, Google/passkey profile persistence, keyboard/pointer UX, cancellation and recovery. Complete the existing 60-minute run and 24-case three-trial comparison requirements, then obtain independent acceptance of the exact delivered revisions.

No gate is waived by this ordering. No source test, present app bundle or successful build is a substitute for a real account or provider workflow. Work toward this finite closeout list before expanding the product scope.

## Packaging closeout checkpoint

Orca runtime packaging is committed at `fede5c6faa4d7d8e22033db9795f609c9807b6a1`; the recorded source regression now comprises 696 distinct cases, including 13 packaging cases. The subsequent build-only change selects a fresh candidate, skips process inspection/stopping/launching in build-only mode, and passes that exact candidate to archive wrappers. Ten independently authored path-selection tests pass, covering existing outputs, app ancestors/aliases, newline paths and unique default candidates. Shell syntax checks pass. Full mocked build/wrapper execution, native compilation and installed-app acceptance remain pending; these helper tests do not prove those gates. No new native candidate was installed and no remote write was performed.
