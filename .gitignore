# deps
node_modules
.pnp
.pnp.js

# build
.next/
out/
build/
dist/
dist-swarm/

# env — NEVER commit real secrets
.env
.env.local
.env.*.local

# prisma generated
prisma/generated/

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# testing
coverage/
.jest-cache/

# editor
.vscode/settings.json
.idea/

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
dist/desktop/

# hades default data dir (workspace store, journals, actor identities) —
# created by `hades …` runs with no HADES_DATA_DIR set; never source
.hades/

# hades CLI bundle (built by scripts/build-hades.mjs)
dist-hades/

# Native Mac build products
dist-mac/
dist/runtime/
src-tauri/target/
src-tauri/gen/

# Retained upstream research snapshots and evidence archives (local only).
.research/

# Built OpenCode fork UI and optional local source checkout.
dist/helm-ui/
vendor/opencode/

# Local review captures from other product repositories are evidence, not Hades source.
docs/plugins/evidence/*.source
